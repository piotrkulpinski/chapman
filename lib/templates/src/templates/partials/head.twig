<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ pageName }} - <%= name %></title>
  <link rel="stylesheet" media="all" href="styles/main.min.css?v={{ 'now' | date('U') }}"><% if (features.has_modernizr) { %>

  <script src="scripts/modernizr.min.js"></script><% } %>
</head>
